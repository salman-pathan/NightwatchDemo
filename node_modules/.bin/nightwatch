#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nightwatch/bin/nightwatch" "$@"
  ret=$?
else 
  node  "$basedir/../nightwatch/bin/nightwatch" "$@"
  ret=$?
fi
exit $ret
